# Dotfiles Project Notes

## Git

- In this repo, default to commit subjects in the existing `[scope] Summary` style (e.g. `[nvim] ...`, `[dotfiles] ...`, `[claude] ...`) and follow that convention without re-checking recent history unless the user asks for a different style.
